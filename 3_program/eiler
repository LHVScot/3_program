#include <iostream>
#include <locale>
#include <cmath> // Для математических функций (например, sqrt, pow, sin и т.д.)

using namespace std;

// Определяем функцию f(x, y), которую нужно использовать в методе Эйлера
double f(double x, double y) {
  //  Здесь нужно написать функцию, которая соответствует вашему заданию
  //  Пример: f(x, y) = x + y;
    return x*x+y;
}

int main() {
  setlocale(LC_ALL, "ru_RU.UTF-8");

  double x, y, h, b;

  // Ввод данных
  cout << "Введите начальное значение x: ";
  cin >> x;
  cout << "Введите начальное значение y: ";
  cin >> y;
  cout << "Введите шаг h: ";
  cin >> h;
  cout << "Введите конечное значение b: ";
  cin >> b;


  // Проверка условия цикла и вычисления
  while (x <= b) {
    y = y + h * f(x, y);  // Метод Эйлера
    x = x + h;
    cout << "y = " << y << endl; // Вывод текущего значения y
  }

  cout << "Расчеты завершены." << endl;

  return 0;
}
